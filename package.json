{
    "name": "Schach",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build:prod":"bun run --filter '@schach/frontend' build:prod",
        "build:dev":"bun run --filter '@schach/frontend' build:dev"
    }
}
